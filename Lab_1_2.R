#Creating a vector named num with four elements (2, 0, 4, 6). 

num <- c(2,0,4,6)

#Displaying the thrid element
num[3]

#Displaying all the element except the first
num[-1]

#Counting the length of the vector
length(num)
